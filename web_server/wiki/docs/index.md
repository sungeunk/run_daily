# run_daily Wiki

Welcome to the run_daily documentation.

Use the navigation to find operational notes and procedures.

## Editing workflow

1. Edit Markdown files under `docs/`.
2. Build the site with `mkdocs build --strict`.
3. Review the generated site locally.
4. Commit the Markdown and configuration changes to Git.
